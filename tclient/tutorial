package main

import (
	"fmt"
	"xxx/tapi/sharemgnt"

	"github.com/kweaver-ai/idrm-go-common/tclient"
)

func main() {
	var client *sharemgnt.NcTShareMgntClient
	err := tclient.NewTClient(sharemgnt.NewNcTShareMgntClientFactory, &client, "localhost", sharemgnt.NCT_SHAREMGNT_PORT)
	if err != nil {
		fmt.Println(err)
		return
	}
	defer client.Transport.Close()
	result, err := client.GetCSFLevels()
	if err != nil {
		fmt.Println(err)
		return
	}
	fmt.Println(result)
	return
}
